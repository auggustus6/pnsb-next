import { ReactNode } from "react";
import * as S from './styles'

type {{pascalCase name}}Props = {
  children?: ReactNode;
  className?: string;
}

const {{pascalCase name}} = ({children, className}:{{pascalCase name}}Props) => {
  return(
    <S.Wrapper className={className}>
      {children}
    </S.Wrapper>
  )
};

export default {{pascalCase name}}
